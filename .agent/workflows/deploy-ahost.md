---
description: Инструкция по деплою проекта INTRA на ahost
---

# 🚀 Деплой проекта INTRA на Ahost

Полная инструкция по развёртыванию Vue.js + Node.js/Express приложения на хостинге ahost.uz.

---

## 📋 Предварительные требования

- Аккаунт на ahost.uz с поддержкой Node.js
- Доступ к панели управления (cPanel/DirectAdmin)
- База данных MySQL
- SSH доступ (рекомендуется)

---

## 🔧 Шаг 1: Подготовка проекта локально

### 1.1. Сборка frontend

```bash
cd /Users/macbookair/projects/intra/intra-v2
npm run build
```

Это создаст папку `dist/` со статическими файлами.

### 1.2. Проверка структуры для выгрузки

Убедитесь, что у вас есть:
- `dist/` - собранный frontend
- `server/` - backend сервер
- `package.json` - зависимости
- `.env.example` - шаблон переменных окружения

---

## 🗄️ Шаг 2: Настройка базы данных на ahost

### 2.1. Создайте базу данных

1. Войдите в панель управления ahost
2. Найдите раздел **«MySQL Базы данных»**
3. Создайте новую базу данных (например: `username_intra`)
4. Создайте пользователя БД (например: `username_intra_user`)
5. Назначьте пользователю **ВСЕ привилегии** на созданную БД

### 2.2. Запишите данные подключения

```
DB_HOST=localhost (или IP сервера MySQL)
DB_PORT=3306
DB_USER=username_intra_user
DB_PASSWORD=ваш_пароль
DB_NAME=username_intra
```

> ⚠️ На ahost обычно `DB_HOST=localhost` работает, если БД на том же сервере.

---

## 📁 Шаг 3: Загрузка файлов на сервер

### Вариант А: Через SSH / SFTP (рекомендуется)

1. Подключитесь к серверу через SSH:
```bash
ssh username@your-domain.uz
```

2. Перейдите в папку приложения:
```bash
cd ~/domains/your-domain.uz/public_html
# или
cd ~/www
```

3. Загрузите файлы через SFTP (FileZilla, WinSCP):

**Загрузите следующие файлы/папки:**
```
├── dist/              (собранный frontend)
├── server/            (backend)
├── package.json
├── package-lock.json
├── .env               (создайте из .env.example)
```

### Вариант Б: Через Git (если есть доступ)

```bash
cd ~/domains/your-domain.uz/public_html
git clone https://github.com/your-username/intra-v2.git .
npm install --production
npm run build
```

---

## ⚙️ Шаг 4: Настройка переменных окружения

Создайте файл `.env` на сервере:

```bash
nano .env
```

Содержимое `.env`:

```env
# ===========================================
# INTRA - Production Configuration
# ===========================================

# Database Configuration (данные от ahost)
DB_HOST=localhost
DB_PORT=3306
DB_USER=username_intra_user
DB_PASSWORD=ваш_надёжный_пароль
DB_NAME=username_intra

# Server Configuration
PORT=3001
NODE_ENV=production

# Frontend URL (ваш домен)
FRONTEND_URL=https://your-domain.uz
```

---

## 📦 Шаг 5: Установка зависимостей

В папке проекта на сервере:

```bash
npm install --production
```

Это установит только production зависимости (без devDependencies).

---

## 🚀 Шаг 6: Запуск приложения

### 6.1. Первый запуск (проверка)

```bash
npm start
```

При первом запуске сервер:
1. Проверит подключение к БД
2. Автоматически создаст необходимые таблицы
3. Запустит Express сервер

Вы должны увидеть:
```
✅ Подключение к MySQL успешно
🚀 СЕРВЕР УСПЕШНО ЗАПУЩЕН
📍 URL: http://localhost:3001
📊 База данных: username_intra
🌍 Режим: PRODUCTION
```

### 6.2. Настройка PM2 (для постоянной работы)

Если на ahost доступен PM2:

```bash
# Установка PM2 глобально
npm install -g pm2

# Запуск приложения
pm2 start server/index.js --name "intra"

# Сохранение конфигурации
pm2 save

# Автозапуск при перезагрузке сервера
pm2 startup
```

**Полезные команды PM2:**
```bash
pm2 status        # Статус приложения
pm2 logs intra    # Логи
pm2 restart intra # Перезапуск
pm2 stop intra    # Остановка
```

---

## 🌐 Шаг 7: Настройка веб-сервера (Nginx/Apache)

### Вариант А: Apache (.htaccess) - типично для ahost

Создайте файл `.htaccess` в корне public_html:

```apache
# Redirect all traffic to Node.js app
RewriteEngine On

# If the request is for a static file, serve it directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Proxy all other requests to Node.js
RewriteRule ^api/(.*)$ http://localhost:3001/api/$1 [P,L]

# SPA fallback
RewriteRule ^ /index.html [L]
```

> ⚠️ Если проксирование не работает, обратитесь в поддержку ahost для настройки.

### Вариант Б: Приложение полностью через Node.js

Если ahost позволяет привязать домен к Node.js порту:

1. В панели управления найдите **«Node.js менеджер»**
2. Создайте новое приложение:
   - **Application Root**: путь к проекту
   - **Application URL**: your-domain.uz
   - **Application Startup File**: `server/index.js`
   - **Environment**: Production
3. Добавьте переменные окружения из `.env`
4. Нажмите **«Create»** / **«Run»**

---

## 🔍 Шаг 8: Проверка работоспособности

### 8.1. Проверка API

```bash
curl https://your-domain.uz/api/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "database": "connected",
  "timestamp": "2025-12-22T12:00:00.000Z"
}
```

### 8.2. Проверка страниц

- Главная: `https://your-domain.uz`
- Админка: `https://your-domain.uz/admin`
- API команды: `https://your-domain.uz/api/team`
- API проектов: `https://your-domain.uz/api/projects`

---

## 🛠️ Устранение проблем

### Ошибка подключения к БД

```
❌ Не удалось подключиться к базе данных
```

**Решение:**
1. Проверьте данные в `.env`
2. Убедитесь, что пользователь БД имеет привилегии
3. Попробуйте `DB_HOST=127.0.0.1` вместо `localhost`

### Порт занят

```
Error: listen EADDRINUSE
```

**Решение:**
1. Измените `PORT=3002` в `.env`
2. Или остановите процесс на порту: `kill $(lsof -t -i:3001)`

### 404 на SPA маршрутах

**Решение:**
Убедитесь, что все запросы (кроме API) перенаправляются на `index.html`. См. настройку `.htaccess` или Nginx.

### Таблицы не создаются

**Решение:**
Выполните миграцию вручную:
```bash
npm run migrate
```

Или импортируйте схему напрямую:
```bash
mysql -u username_intra_user -p username_intra < server/migrations/schema.sql
```

---

## 📊 Мониторинг и обслуживание

### Просмотр логов

```bash
pm2 logs intra --lines 100
```

### Обновление приложения

```bash
# Остановка
pm2 stop intra

# Получение обновлений
git pull origin main

# Установка новых зависимостей
npm install --production

# Пересборка frontend
npm run build

# Перезапуск
pm2 restart intra
```

### Бэкап базы данных

```bash
mysqldump -u username_intra_user -p username_intra > backup_$(date +%Y%m%d).sql
```

---

## 📁 Итоговая структура на сервере

```
public_html/
├── .env                  # Переменные окружения (СЕКРЕТНЫЙ)
├── .htaccess            # Конфигурация Apache (если нужно)
├── package.json
├── package-lock.json
├── node_modules/        # Установленные зависимости
├── dist/                # Собранный frontend
│   ├── index.html
│   └── assets/
└── server/              # Backend
    ├── index.js         # Точка входа сервера
    ├── config/
    │   ├── database.js
    │   └── migrations.js
    ├── routes/
    │   ├── team-members.js
    │   ├── projects.js
    │   ├── gallery.js
    │   └── partners.js
    └── migrations/
        └── schema.sql
```

---

## ✅ Чек-лист деплоя

- [ ] База данных создана и настроена
- [ ] Файлы загружены на сервер
- [ ] `.env` создан с правильными данными
- [ ] Dependencies установлены (`npm install --production`)
- [ ] Frontend собран (`npm run build`)
- [ ] Сервер запущен (PM2 или Node.js manager)
- [ ] Домен настроен на приложение
- [ ] API отвечает (`/api/health`)
- [ ] Фронтенд открывается
- [ ] HTTPS настроен

---

## 📞 Поддержка Ahost

Если возникли проблемы с настройкой:
- Телефон: указан на ahost.uz
- Email: support@ahost.uz
- Telegram: @ahost_support

Спросите о:
- Настройке Node.js приложения
- Проксировании на порт Node.js
- Настройке SSL сертификата

---

*Документация создана: 22 декабря 2025*
