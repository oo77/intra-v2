# 🚀 Миграция на MySQL - Руководство

## ✅ Что было сделано

### 1. Создана структура базы данных
- **gallery** - таблица для изображений галереи
- **projects** - таблица для проектов
- **team_members** - таблица для членов команды

Все таблицы поддерживают многоязычность (русский, английский, узбекский).

### 2. Созданы API endpoints

#### Gallery API (`/api/gallery`)
- `GET /api/gallery` - Получить все изображения
- `GET /api/gallery/:id` - Получить одно изображение
- `POST /api/gallery` - Добавить изображение
- `PUT /api/gallery/:id` - Обновить изображение
- `DELETE /api/gallery/:id` - Удалить изображение

#### Projects API (`/api/projects`)
- `GET /api/projects` - Получить все проекты
- `GET /api/projects/:id` - Получить один проект
- `POST /api/projects` - Добавить проект
- `PUT /api/projects/:id` - Обновить проект
- `DELETE /api/projects/:id` - Удалить проект

#### Team Members API (`/api/team`)
- `GET /api/team` - Получить всех членов команды
- `GET /api/team/:id` - Получить одного члена команды
- `POST /api/team` - Добавить члена команды
- `PUT /api/team/:id` - Обновить члена команды
- `DELETE /api/team/:id` - Удалить члена команды

### 3. Настроена локальная база данных

Из-за недоступности удаленного сервера (83.69.139.250) была настроена локальная MySQL база данных для разработки.

## 📋 Текущая конфигурация

**Локальная база данных:**
- Host: localhost
- Port: 3306
- User: root
- Database: intra_local

**Удаленная база данных (закомментирована в .env.backup):**
- Host: 83.69.139.250
- Port: 3306
- User: intrauz1_admin
- Database: intrauz1_main

## 🔧 Команды для управления

### Запуск сервера API
```bash
node server/index.js
```

### Создание таблиц (миграции)
```bash
node server/migrations/run-migrations.js
```

### Перенос данных из JSON в MySQL
```bash
node server/migrate-data.js
```

### Настройка локальной БД
```bash
./setup-local-db.sh
```

### Проверка подключения к БД
```bash
node server/test-db-connection.js
```

## 🌐 Следующие шаги

### 1. Обновить Pinia stores

Нужно обновить следующие файлы для работы с API вместо JSON:

- `src/stores/gallery.js` - для галереи
- `src/stores/projects.js` - для проектов  
- `src/stores/team.js` - для команды

### 2. Обновить компоненты админ-панели

Обновить компоненты для работы с новым API:

- `src/views/GalleryAdmin.vue`
- `src/views/AboutGalleryAdmin.vue`
- `src/views/TeamAdmin.vue`
- Создать `src/views/ProjectsAdmin.vue`

### 3. Обновить страницы для чтения данных

Обновить страницы для получения данных из API:

- `src/views/About.vue` - галерея
- `src/views/Projects.vue` - проекты
- `src/views/Team.vue` - команда

## 🔐 Переключение на удаленную БД

Когда удаленный сервер станет доступен:

1. Откройте `.env.backup` и скопируйте настройки удаленной БД
2. Вставьте их в `.env`
3. Запустите миграции: `node server/migrations/run-migrations.js`
4. Перенесите данные: `node server/migrate-data.js`

## 📊 Статус миграции

✅ База данных создана  
✅ Таблицы созданы  
✅ Данные перенесены (12 изображений, 6 проектов, 5 членов команды)  
✅ API сервер работает  
⏳ Pinia stores - требуется обновление  
⏳ Компоненты Vue - требуется обновление  

## 🐛 Решение проблем

### Проблема: ETIMEDOUT при подключении к удаленной БД

**Причина:** Удаленный сервер недоступен из вашей сети

**Решение:** 
1. Используйте локальную БД для разработки
2. Проверьте доступность сервера: `ping 83.69.139.250`
3. Возможно требуется VPN или SSH туннель

### Проблема: MySQL сервер не запущен

**Решение:**
```bash
mysql.server start
# или
brew services start mysql
```

### Проблема: Ошибка "row_number" в SQL

**Решение:** Уже исправлено - используется `row_num` вместо зарезервированного слова `row_number`

## 📝 Примеры использования API

### Получить все проекты
```bash
curl http://localhost:3001/api/projects
```

### Получить галерею
```bash
curl http://localhost:3001/api/gallery
```

### Получить команду
```bash
curl http://localhost:3001/api/team
```

### Проверить здоровье сервера
```bash
curl http://localhost:3001/api/health
```
