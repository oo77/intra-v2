# Настройка Telegram Бота для формы Contact

## Информация о боте
- **Токен:** `8335328034:AAFwylv6zS5Gzf9ImqUiVsRwFBgE3aaYQk8`
- **Имя бота:** `intra_notice_bot`

## Шаги настройки

### 1. Получение Chat ID

Для работы бота нужно узнать `chat_id` куда отправлять сообщения:

#### Вариант A: Личные сообщения боту
1. Откройте бота в Telegram: [@intra_notice_bot](https://t.me/intra_notice_bot)
2. Нажмите `/start`
3. Откройте в браузере: `https://api.telegram.org/bot8335328034:AAFwylv6zS5Gzf9ImqUiVsRwFBgE3aaYQk8/getUpdates`
4. Найдите в ответе `"chat":{"id":XXXXXXX}` - это ваш chat_id
5. Замените в коде `chatId` на полученное число

#### Вариант B: Отправка в канал/группу
1. Создайте канал или группу в Telegram
2. Добавьте бота `@intra_notice_bot` как администратора
3. Отправьте любое сообщение в канал/группу
4. Откройте: `https://api.telegram.org/bot8335328034:AAFwylv6zS5Gzf9ImqUiVsRwFBgE3aaYQk8/getUpdates`
5. Найдите `"chat":{"id":-XXXXXXXXX}` (отрицательное число для групп/каналов)
6. Используйте это число как `chatId`

### 2. Обновление кода

После получения chat_id, обновите файл `src/views/Contacts.vue`:

```javascript
const chatId = 'ВАШ_CHAT_ID' // например: 123456789 или -1001234567890
```

### 3. Формат сообщений

Бот отправляет сообщения в следующем формате:

```
🔔 Новое сообщение с сайта

👤 Имя: [имя пользователя]
📧 Email: [email]
🏢 Организация: [организация] (если указана)
📱 Телефон: [телефон] (если указан)
📝 Тема: [тема сообщения]

💬 Сообщение:
[текст сообщения]

⏰ Время: [дата и время]
```

### 4. Тестирование

1. Откройте сайт на странице Contact
2. Заполните форму
3. Нажмите "Отправить"
4. Проверьте, что сообщение пришло в Telegram

### 5. Безопасность

⚠️ **ВАЖНО:** Токен бота сейчас находится в клиентском коде, что небезопасно!

**Рекомендации:**
- Создайте backend API endpoint для отправки сообщений
- Храните токен бота на сервере, а не в клиентском коде
- Добавьте rate limiting для предотвращения спама

## Альтернативное решение (более безопасное)

Создайте простой backend endpoint:

```javascript
// backend/api/send-telegram.js
export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' })
  }

  const { name, email, subject, message, organization, phone } = req.body
  
  const botToken = process.env.TELEGRAM_BOT_TOKEN
  const chatId = process.env.TELEGRAM_CHAT_ID
  
  const telegramMessage = `
🔔 *Новое сообщение с сайта*

👤 *Имя:* ${name}
📧 *Email:* ${email}
${organization ? `🏢 *Организация:* ${organization}` : ''}
${phone ? `📱 *Телефон:* ${phone}` : ''}
📝 *Тема:* ${subject}

💬 *Сообщение:*
${message}

⏰ *Время:* ${new Date().toLocaleString('ru-RU', { timeZone: 'Asia/Tashkent' })}
  `.trim()
  
  try {
    const response = await fetch(`https://api.telegram.org/bot${botToken}/sendMessage`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        chat_id: chatId,
        text: telegramMessage,
        parse_mode: 'Markdown'
      })
    })
    
    const data = await response.json()
    
    if (!data.ok) {
      throw new Error(data.description)
    }
    
    res.status(200).json({ success: true })
  } catch (error) {
    res.status(500).json({ error: error.message })
  }
}
```

Тогда в `.env`:
```
TELEGRAM_BOT_TOKEN=8335328034:AAFwylv6zS5Gzf9ImqUiVsRwFBgE3aaYQk8
TELEGRAM_CHAT_ID=ваш_chat_id
```
