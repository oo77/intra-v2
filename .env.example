# ===========================================
# INTRA - Настройки окружения
# ===========================================

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name

# Server Configuration
PORT=3001
NODE_ENV=production

# Frontend URL (для CORS, если нужно)
# FRONTEND_URL=https://your-domain.uz

# API URL (опционально, по умолчанию /api)
# Раскомментируйте если API на другом домене
# VITE_API_URL=https://api.your-domain.uz