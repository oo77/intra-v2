# 🚀 Система автоматической миграции БД - Краткое руководство

## ✨ Основные возможности

Сервер **автоматически проверяет и создаёт** все необходимые таблицы при запуске!

## 📋 Использование

### Запуск сервера (миграция выполняется автоматически)

```bash
# Development
npm run server

# Production
npm start
```

При запуске сервера автоматически:
1. Проверяется подключение к БД
2. Проверяется наличие всех таблиц
3. Создаются отсутствующие таблицы
4. Сервер запускается

### Ручной запуск миграции (опционально)

```bash
# Проверка + создание отсутствующих таблиц
npm run migrate

# Принудительная миграция (используйте с осторожностью)
npm run migrate:force
```

## 🎯 Что происходит при запуске

### ✅ Все таблицы существуют
```
🔍 Проверка структуры базы данных...
✅ Таблица "team_members" существует
✅ Таблица "projects" существует
✅ Таблица "gallery" существует
✅ Таблица "partners" существует
✅ Все таблицы присутствуют в базе данных
🚀 СЕРВЕР УСПЕШНО ЗАПУЩЕН
```

### ⚙️ Некоторые таблицы отсутствуют (автоматическое создание)
```
🔍 Проверка структуры базы данных...
⚠️  Таблица "projects" не найдена
⚠️  Таблица "gallery" не найдена

🔧 Запуск автоматической миграции...
📝 Выполнение SQL команд...
✅ Миграция успешно выполнена!
✅ База данных успешно инициализирована!
🚀 СЕРВЕР УСПЕШНО ЗАПУЩЕН
```

## 📦 Необходимые таблицы

Система автоматически проверяет и создаёт:
- **team_members** - Данные о команде
- **projects** - Проекты
- **gallery** - Галерея изображений
- **partners** - Партнёры

## ⚙️ Настройка

Убедитесь, что `.env` содержит настройки БД:

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=intra_local
```

## 📚 Подробная документация

Смотрите: [server/migrations/README.md](./migrations/README.md)

## 🔧 Добавление новой таблицы

1. Добавьте SQL в `server/migrations/schema.sql`
2. Добавьте имя в `REQUIRED_TABLES` в `server/config/migrations.js`
3. Перезапустите сервер - таблица создастся автоматически!

## ✅ Преимущества

- ✨ **Автоматическая** инициализация при запуске
- 🛡️ **Безопасная** - не трогает существующие данные
- 📊 **Информативная** - подробные логи
- 🚀 **Production-ready** - готово к деплою

---

**Больше не нужно вручную запускать миграции!** 🎉
