# 🗄️ Автоматическая миграция базы данных

## Описание

Система автоматической проверки и миграции базы данных для проекта Intra v2.

## Как это работает

### Автоматическая миграция при запуске сервера

При каждом запуске сервера (`npm run server` или `npm start`) система автоматически:

1. ✅ **Проверяет подключение** к базе данных
2. 🔍 **Проверяет наличие** всех необходимых таблиц:
   - `team_members` - Команда
   - `projects` - Проекты
   - `gallery` - Галерея
   - `partners` - Партнёры
3. 🚀 **Автоматически создаёт** отсутствующие таблицы из `server/migrations/schema.sql`
4. ✅ **Подтверждает** успешную инициализацию

### Что происходит в разных сценариях

#### Сценарий 1: Все таблицы существуют
```
🔍 Проверка структуры базы данных...
✅ Таблица "team_members" существует
✅ Таблица "projects" существует
✅ Таблица "gallery" существует
✅ Таблица "partners" существует

✅ Все таблицы присутствуют в базе данных
🚀 Сервер запущен: http://localhost:3001
```

#### Сценарий 2: Некоторые таблицы отсутствуют
```
🔍 Проверка структуры базы данных...
✅ Таблица "team_members" существует
⚠️  Таблица "projects" не найдена
⚠️  Таблица "gallery" не найдена
✅ Таблица "partners" существует

⚠️  Обнаружено отсутствующих таблиц: 2
📋 Отсутствующие таблицы: projects, gallery

🔧 Запуск автоматической миграции...
🚀 Запуск миграции базы данных...
📝 Выполнение SQL команд...
✅ Миграция успешно выполнена!
📊 Созданы таблицы:
   - team_members
   - projects
   - gallery
   - partners

✅ База данных успешно инициализирована!
🚀 Сервер запущен: http://localhost:3001
```

## Ручной запуск миграции

### Обычная миграция (проверка + создание)
```bash
npm run migrate
```

Эта команда:
- Проверяет существующие таблицы
- Создаёт только отсутствующие

### Принудительная миграция
```bash
npm run migrate:force
```

⚠️ **ВНИМАНИЕ**: Эта команда выполняет миграцию без проверки!

## Добавление новых таблиц

Чтобы добавить новую таблицу в систему автоматической миграции:

1. **Обновите `server/migrations/schema.sql`**:
   ```sql
   CREATE TABLE IF NOT EXISTS your_new_table (
       id INT PRIMARY KEY AUTO_INCREMENT,
       -- ваши поля
   ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
   ```

2. **Добавьте таблицу в список `REQUIRED_TABLES`** в файле `server/config/migrations.js`:
   ```javascript
   const REQUIRED_TABLES = [
       'team_members',
       'projects',
       'gallery',
       'partners',
       'your_new_table'  // добавьте сюда
   ];
   ```

3. **Перезапустите сервер** - таблица создастся автоматически!

## Структура файлов

```
server/
├── config/
│   ├── database.js       # Подключение к БД
│   └── migrations.js     # Логика автоматической миграции
├── migrations/
│   └── schema.sql        # SQL схема базы данных
├── index.js              # Главный файл сервера
└── migrate.js            # Утилита для ручного запуска
```

## Настройка подключения к БД

Убедитесь, что файл `.env` содержит правильные настройки:

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=intra_local
```

## Решение проблем

### Ошибка подключения к БД
```
❌ Не удалось подключиться к базе данных
💡 Проверьте настройки подключения в .env файле
```

**Решение**: Проверьте настройки в `.env` и убедитесь, что MySQL сервер запущен.

### Ошибка выполнения миграции
```
❌ Ошибка при выполнении миграции:
   Сообщение: [детали ошибки]
```

**Решение**: 
1. Проверьте синтаксис SQL в `server/migrations/schema.sql`
2. Убедитесь, что у пользователя БД есть права на CREATE TABLE
3. Проверьте логи для подробной информации

## Преимущества системы

✅ **Автоматическая**: Не нужно помнить о миграциях  
✅ **Безопасная**: Использует `CREATE TABLE IF NOT EXISTS`  
✅ **Информативная**: Подробные логи на каждом этапе  
✅ **Гибкая**: Можно запустить вручную при необходимости  
✅ **Production-ready**: Работает в любом окружении  

## Логи

Система выводит подробные логи:
- 🔍 Проверка таблиц
- 📝 Выполнение команд
- ✅ Успех операций
- ❌ Ошибки с деталями

Это помогает быстро диагностировать проблемы.

## Безопасность

- ✅ Используется `CREATE TABLE IF NOT EXISTS` - существующие таблицы не затрагиваются
- ✅ Данные в существующих таблицах сохраняются
- ✅ Транзакции не используются для DDL (это ограничение MySQL)
- ⚠️ Всегда делайте бэкап перед production миграцией!

## Поддержка production

Система полностью готова для production:

```bash
# Production запуск с автоматической миграцией
NODE_ENV=production npm start
```

При деплое на сервер (ahost, VPS и т.д.):
1. Настройте `.env` с production данными
2. Запустите сервер - миграция выполнится автоматически
3. Проверьте логи для подтверждения

---

**Разработано для Intra v2** 🚀
